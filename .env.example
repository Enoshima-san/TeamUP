# проект учебный, тут секреты

# Application
APP_NAME=TeamFinder
APP_ENV=development
APP_DEBUG=True
APP_HOST=localhost
APP_PORT=8000

# Security
SECURITY_ALGORITHM=HS256
SECURITY_SECRET_KEY=super-secret-key

# Api (сомнительно это хранить конечно, вроде надо знать какие эндпоинты мы юзаем,
#   и хранить их в каком-то общем доступе, а вроде лишнее вообще, пока закомментим)
# API_REGISTRATION=auth
# API_SELF_USER_PROFILE=my

# Auth
# пока не придумал

# Database
DB_DRIVER=postgresql+asyncpg
DB_HOST=localhost
DB_PORT=5432
DB_NAME=team_finder
DB_USER=postgres
DB_PASSWORD=postgres # Просто тестовый пароль

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
